const colors = {
  black: "#000000",
  blue: "#0F52BA",
  green: "#009E60",
  grey: "#444444",
  red: "#D2042D",
  white: "#FFFFFF",
  lightGrey: "#999999",
  translucentGrey: "#00000008",
};

const fontSizes = {
  small: 14,
  medium: 18,
  large: 36,
};

const image = {
  height: 40,
  width: 40,
};

const margins = {
  small: 4,
  medium: 8,
  large: 16,
};

const paddings = {
  small: 4,
  medium: 12,
  large: 16,
};

const roundedComponent = {
  borderRadius: 8,
};

const theme = {
  colors,
  fontSizes,
  image,
  margins,
  paddings,
  roundedComponent,
};

export default theme;
